# 0.14.1 – Personal Heatmap Preview & Animated Borders

**Release Date:** 2025-01-XX

## ✨ New Features

### Personal Heatmap Preview GUI
- **Visual Activity Map**: 7x5 grid showing area activity intensity around the player
- **Multiple Activity Types**: View movement, deaths, kills, mining, or building activity
- **Color Gradient**: Heat intensity from gray (no activity) → green → yellow → orange → red → purple (high)
- **Activity Type Cycling**: Click compass (slot 0) to switch between activity views
- **World Selection**: Click grass block (slot 8) to cycle through dimensions
- **Player Marker**: Your current chunk is highlighted with a player head
- **Player Integration**: Access from PlayerStatsGui navigation bar (slot 52)

### Animated Borders
- **Rainbow Animation**: Cycling border colors for GUI visual polish
- **Configurable**: Can be enabled/disabled in config.yml (`gui.animated_borders: true`)
- **MainMenuGui Integration**: Animated borders around main menu items
- **Performance**: Uses BukkitScheduler with configurable tick interval

## 🔧 Technical Details

- New `PersonalHeatmapGui` class with 35-cell visual grid (slots 10-44)
- New `AnimatedBorderService` for managing animation registrations
- Activity intensity calculated from chunk-based heatmap data (last 7 days)
- Normalizes intensity to 10 levels with distinct color coding
- Config version bumped to 7

## 🧪 Testing

- 12 new tests in `PersonalHeatmapGuiTest`
- 16 new tests in `AnimatedBorderServiceTest`
- All 678 tests passing (7 skipped)

## 📦 Dependencies

- No new dependencies
- Uses existing HeatmapService infrastructure
